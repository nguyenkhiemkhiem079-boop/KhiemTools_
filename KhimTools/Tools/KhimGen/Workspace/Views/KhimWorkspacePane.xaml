<Page x:Class="KhimTools.Tools.Workspace.Views.KhimWorkspacePane"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="320"
      Title="KhimTools Workspace"
      Background="#F8FAFC"
      FontFamily="Segoe UI">

    <Page.Resources>
        <LinearGradientBrush x:Key="BrandGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#1E40AF" Offset="0"/>
            <GradientStop Color="#3B82F6" Offset="1"/>
        </LinearGradientBrush>

        <!-- Quick Action Card Style -->
        <Style x:Key="ActionCardButton" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="cardBorder" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="10" 
                                Padding="12,10">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="4" Direction="270" ShadowDepth="1" Opacity="0.05" Color="#000000"/>
                            </Border.Effect>
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="cardBorder" Property="Background" Value="#F1F5F9"/>
                                <Setter TargetName="cardBorder" Property="BorderBrush" Value="#93C5FD"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="cardBorder" Property="Background" Value="#E2E8F0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="12,14,12,20">

                <!-- 1. HEADER & BRANDING -->
                <Border Background="{StaticResource BrandGradient}" CornerRadius="12" Padding="14,12" Margin="0,0,0,14">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Width="36" Height="36" CornerRadius="8" Background="White">
                            <TextBlock Text="K" FontSize="20" FontWeight="Bold" Foreground="#1E40AF" 
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>

                        <StackPanel Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="KHIM WORKSPACE" FontSize="13" FontWeight="Bold" Foreground="White"/>
                            <TextBlock Text="BIM Engineering Suite" FontSize="11" Foreground="#BFDBFE"/>
                        </StackPanel>

                        <Button Grid.Column="2" Command="{Binding CheckUpdateCommand}" 
                                ToolTip="Kiểm tra cập nhật" Background="#2563EB" BorderThickness="0" 
                                Cursor="Hand" Padding="6,4">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="#1D4ED8" CornerRadius="6" Padding="6,3">
                                        <TextBlock Text="v2.0" Foreground="White" FontSize="11" FontWeight="Bold"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>
                </Border>

                <!-- 2. REBAR WORKFLOW SECTION -->
                <TextBlock Text="THIẾT KẾ CỐT THÉP (REBAR)" FontSize="11" FontWeight="Bold" Foreground="#64748B" Margin="2,0,0,6"/>

                <Button Style="{StaticResource ActionCardButton}" Command="{Binding RunColumnRebarCommand}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="🏛" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Column Rebar 2.0" FontSize="13" FontWeight="SemiBold" Foreground="#1E293B"/>
                            <TextBlock Text="Bố trí thép Cột Vuông, Tròn &amp; Xuất bản vẽ" FontSize="11" Foreground="#64748B"/>
                        </StackPanel>
                        <TextBlock Grid.Column="2" Text="➔" FontSize="14" Foreground="#94A3B8" VerticalAlignment="Center"/>
                    </Grid>
                </Button>

                <Button Style="{StaticResource ActionCardButton}" Command="{Binding RunBeamRebarCommand}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="🏗" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Beam Rebar 2.0" FontSize="13" FontWeight="SemiBold" Foreground="#1E293B"/>
                            <TextBlock Text="Thép dầm liên tục TCVN / Eurocode" FontSize="11" Foreground="#64748B"/>
                        </StackPanel>
                        <TextBlock Grid.Column="2" Text="➔" FontSize="14" Foreground="#94A3B8" VerticalAlignment="Center"/>
                    </Grid>
                </Button>

                <Button Style="{StaticResource ActionCardButton}" Command="{Binding RunSlabRebarCommand}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="🔲" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Slab Rebar" FontSize="13" FontWeight="SemiBold" Foreground="#1E293B"/>
                            <TextBlock Text="Thép sàn 2 lớp, thép mũ &amp; gia cường" FontSize="11" Foreground="#64748B"/>
                        </StackPanel>
                        <TextBlock Grid.Column="2" Text="➔" FontSize="14" Foreground="#94A3B8" VerticalAlignment="Center"/>
                    </Grid>
                </Button>

                <Button Style="{StaticResource ActionCardButton}" Command="{Binding RunFoundationRebarCommand}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="🧱" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Foundation Rebar" FontSize="13" FontWeight="SemiBold" Foreground="#1E293B"/>
                            <TextBlock Text="Thép đài móng, móng đơn &amp; râu chờ" FontSize="11" Foreground="#64748B"/>
                        </StackPanel>
                        <TextBlock Grid.Column="2" Text="➔" FontSize="14" Foreground="#94A3B8" VerticalAlignment="Center"/>
                    </Grid>
                </Button>

                <!-- 3. GEOMETRY SECTION -->
                <TextBlock Text="HÌNH HỌC &amp; JOIN (GEOMETRY)" FontSize="11" FontWeight="Bold" Foreground="#64748B" Margin="2,10,0,6"/>

                <Button Style="{StaticResource ActionCardButton}" Command="{Binding RunJoinElementsCommand}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="🔗" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Join Elements Manager" FontSize="13" FontWeight="SemiBold" Foreground="#1E293B"/>
                            <TextBlock Text="Join / Unjoin / Switch đa năng đa Category" FontSize="11" Foreground="#64748B"/>
                        </StackPanel>
                        <TextBlock Grid.Column="2" Text="➔" FontSize="14" Foreground="#94A3B8" VerticalAlignment="Center"/>
                    </Grid>
                </Button>

                <!-- 4. ARCHITECTURAL SECTION -->
                <TextBlock Text="KIẾN TRÚC (ARCHITECTURAL)" FontSize="11" FontWeight="Bold" Foreground="#64748B" Margin="2,10,0,6"/>

                <Button Style="{StaticResource ActionCardButton}" Command="{Binding RunRoom3DViewCommand}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="🏢" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Room 3D View Generator" FontSize="13" FontWeight="SemiBold" Foreground="#1E293B"/>
                            <TextBlock Text="Tự động tạo 3D Section Box cô lập theo phòng" FontSize="11" Foreground="#64748B"/>
                        </StackPanel>
                        <TextBlock Grid.Column="2" Text="➔" FontSize="14" Foreground="#94A3B8" VerticalAlignment="Center"/>
                    </Grid>
                </Button>

                <Button Style="{StaticResource ActionCardButton}" Command="{Binding RunWallFloorFinishesCommand}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="🏠" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Room Finishes" FontSize="13" FontWeight="SemiBold" Foreground="#1E293B"/>
                            <TextBlock Text="Lát sàn &amp; ốp chân tường theo biên dạng phòng" FontSize="11" Foreground="#64748B"/>
                        </StackPanel>
                        <TextBlock Grid.Column="2" Text="➔" FontSize="14" Foreground="#94A3B8" VerticalAlignment="Center"/>
                    </Grid>
                </Button>

                <!-- 5. MEP ENGINEERING SECTION -->
                <TextBlock Text="CƠ ĐIỆN (MEP ENGINEERING)" FontSize="11" FontWeight="Bold" Foreground="#64748B" Margin="2,10,0,6"/>

                <Button Style="{StaticResource ActionCardButton}" Command="{Binding RunMepOpeningsCommand}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="🕳" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="MEP Openings" FontSize="13" FontWeight="SemiBold" Foreground="#1E293B"/>
                            <TextBlock Text="Quét va chạm và tự động đục lỗ mở dầm/sàn/vách" FontSize="11" Foreground="#64748B"/>
                        </StackPanel>
                        <TextBlock Grid.Column="2" Text="➔" FontSize="14" Foreground="#94A3B8" VerticalAlignment="Center"/>
                    </Grid>
                </Button>

                <Button Style="{StaticResource ActionCardButton}" Command="{Binding RunMepElevationTagsCommand}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="🏷" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="MEP Elevation Tags" FontSize="13" FontWeight="SemiBold" Foreground="#1E293B"/>
                            <TextBlock Text="Gán Tag cao độ đáy BOP / Invert cho ống gió &amp; ống nước" FontSize="11" Foreground="#64748B"/>
                        </StackPanel>
                        <TextBlock Grid.Column="2" Text="➔" FontSize="14" Foreground="#94A3B8" VerticalAlignment="Center"/>
                    </Grid>
                </Button>

                <!-- 6. DOCUMENTATION SECTION -->
                <TextBlock Text="TRIỂN KHAI BẢN VẼ (DOCUMENTATION)" FontSize="11" FontWeight="Bold" Foreground="#64748B" Margin="2,10,0,6"/>

                <Button Style="{StaticResource ActionCardButton}" Command="{Binding RunSectionCutCommand}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="✂" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Smart Section Cut" FontSize="13" FontWeight="SemiBold" Foreground="#1E293B"/>
                            <TextBlock Text="Tự động cắt Section dọc/ngang cấu kiện" FontSize="11" Foreground="#64748B"/>
                        </StackPanel>
                        <TextBlock Grid.Column="2" Text="➔" FontSize="14" Foreground="#94A3B8" VerticalAlignment="Center"/>
                    </Grid>
                </Button>

                <Button Style="{StaticResource ActionCardButton}" Command="{Binding RunAlignViewportCommand}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="📐" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Align Viewports" FontSize="13" FontWeight="SemiBold" Foreground="#1E293B"/>
                            <TextBlock Text="Căn chỉnh vị trí khung nhìn chuẩn xác" FontSize="11" Foreground="#64748B"/>
                        </StackPanel>
                        <TextBlock Grid.Column="2" Text="➔" FontSize="14" Foreground="#94A3B8" VerticalAlignment="Center"/>
                    </Grid>
                </Button>

                <Button Style="{StaticResource ActionCardButton}" Command="{Binding RunUpdateDetailNoCommand}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="🔢" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Update Detail Numbers" FontSize="13" FontWeight="SemiBold" Foreground="#1E293B"/>
                            <TextBlock Text="Đồng bộ số hiệu chi tiết từ tên View" FontSize="11" Foreground="#64748B"/>
                        </StackPanel>
                        <TextBlock Grid.Column="2" Text="➔" FontSize="14" Foreground="#94A3B8" VerticalAlignment="Center"/>
                    </Grid>
                </Button>

                <Button Style="{StaticResource ActionCardButton}" Command="{Binding RunSheetExportCommand}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="🖨" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Sheet Exporter Pro" FontSize="13" FontWeight="SemiBold" Foreground="#1E293B"/>
                            <TextBlock Text="In PDF/DWG hàng loạt &amp; Quản lý Issue" FontSize="11" Foreground="#64748B"/>
                        </StackPanel>
                        <TextBlock Grid.Column="2" Text="➔" FontSize="14" Foreground="#94A3B8" VerticalAlignment="Center"/>
                    </Grid>
                </Button>

                <!-- 5. FOOTER STATUS BAR -->
                <Border Background="#E2E8F0" CornerRadius="8" Padding="10,6" Margin="0,10,0,0">
                    <TextBlock Text="{Binding StatusMessage}" FontSize="11" Foreground="#475569" TextTrimming="CharacterEllipsis"/>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
