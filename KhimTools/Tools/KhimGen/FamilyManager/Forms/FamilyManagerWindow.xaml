<Window x:Class="KhimTools.FamilyManager.Forms.FamilyManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="K-TOOLS — Family Manager"
        Height="560" Width="820" MinWidth="720" MinHeight="420"
        WindowStartupLocation="CenterScreen"
        Background="#F8FAFC"
        FontFamily="Segoe UI"
        ResizeMode="CanResizeWithGrip">

    <Window.Resources>
        <Style x:Key="FamilyCell" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="ToolTip" Value="{Binding Text, RelativeSource={RelativeSource Self}}"/>
        </Style>


        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#1677D2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="14,7"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1262B0"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#E2E8F0"/>
            <Setter Property="Foreground" Value="#1E293B"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#CBD5E1"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 2. TOOLBAR (Search & Category Filter) -->
        <Border Grid.Row="1" Background="White" BorderBrush="#E2E8F0" BorderThickness="0,0,0,1" Padding="16,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Search Input -->
                <TextBox x:Name="TxtSearch" Grid.Column="0" Height="32" VerticalContentAlignment="Center"
                         Padding="8,0" Margin="0,0,10,0" FontSize="12"
                         TextChanged="TxtSearch_TextChanged"
                         ToolTip="Tìm kiếm tên Family..." />

                <!-- Category Combo -->
                <ComboBox x:Name="CboCategory" Grid.Column="1" Width="140" Height="32"
                          VerticalContentAlignment="Center" Margin="0,0,10,0"
                          SelectionChanged="CboCategory_SelectionChanged" />

                <!-- Refresh Button -->
                <Button x:Name="BtnRefresh" Grid.Column="2" Content="Làm mới" Style="{StaticResource SecondaryButton}"
                        Click="BtnRefresh_Click" />
            </Grid>
        </Border>

        <!-- 3. DATA GRID -->
        <DataGrid x:Name="GridFamilies" Grid.Row="2" AutoGenerateColumns="False"
                  CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True"
                  SelectionMode="Extended" GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                  MouseDoubleClick="GridFamilies_MouseDoubleClick"
                  HorizontalGridLinesBrush="#F1F5F9"
                  Background="White" BorderThickness="0" Margin="16,12,16,12"
                  RowHeight="36">
            <DataGrid.Columns>
                <DataGridTextColumn ElementStyle="{StaticResource FamilyCell}" Header="Tên Family" Binding="{Binding Name}" Width="200" MinWidth="200" FontWeight="SemiBold"/>
                <DataGridTextColumn ElementStyle="{StaticResource FamilyCell}" Header="Phân Loại" Binding="{Binding Category}" Width="100" MinWidth="100"/>
                <DataGridTemplateColumn Header="Trạng Thái Trong Dự Án" Width="170" MinWidth="170">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="4" Padding="6,2" HorizontalAlignment="Left" VerticalAlignment="Center">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#EEF2F5"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsLoadedInDocument}" Value="True">
                                                <Setter Property="Background" Value="#DCFCE7"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock FontSize="11" FontWeight="Bold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="Chưa nạp"/>
                                            <Setter Property="Foreground" Value="#526170"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsLoadedInDocument}" Value="True">
                                                <Setter Property="Text" Value="Đã nạp"/>
                                                    <Setter Property="Foreground" Value="#166534"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn ElementStyle="{StaticResource FamilyCell}" Header="Số Types" Binding="{Binding SymbolCount}" Width="80" MinWidth="80"/>
                <DataGridTextColumn ElementStyle="{StaticResource FamilyCell}" Header="Dung Lượng" Binding="{Binding FormattedSize}" Width="90" MinWidth="90"/>
                <DataGridTextColumn ElementStyle="{StaticResource FamilyCell}" Header="Đường dẫn" Binding="{Binding FullPath}" Width="*" MinWidth="160"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 4. BOTTOM ACTION BAR -->
        <Border Grid.Row="3" Background="White" BorderBrush="#E2E8F0" BorderThickness="0,1,0,0" Padding="16,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button x:Name="BtnBrowse" Content="Chọn file .rfa" Style="{StaticResource SecondaryButton}"
                            Margin="0,0,8,0" Click="BtnBrowse_Click"/>
                    <Button x:Name="BtnOpenFolder" Content="Mở thư mục" Style="{StaticResource SecondaryButton}"
                            Margin="0,0,8,0" Click="BtnOpenFolder_Click"/>
                </StackPanel>

                <TextBlock x:Name="TxtTotalFamilies" Grid.Column="1" VerticalAlignment="Center" Margin="8,0" Foreground="#5F6B78" TextTrimming="CharacterEllipsis"/>
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button x:Name="BtnReload" Content="Nạp lại &amp; ghi đè" Style="{StaticResource SecondaryButton}"
                            Margin="0,0,8,0" Click="BtnReload_Click"/>
                    <Button x:Name="BtnLoad" Content="Nạp Family" Style="{StaticResource ModernButton}"
                            Margin="0,0,8,0" Click="BtnLoad_Click"/>
                    <Button x:Name="BtnClose" Content="Đóng" Style="{StaticResource SecondaryButton}"
                            Click="BtnClose_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
