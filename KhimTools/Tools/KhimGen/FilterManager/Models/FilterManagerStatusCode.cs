namespace KhimTools.FilterManager.Models
{
    public enum FilterManagerStatusCode
    {
        READY,
        NO_CHANGE,
        SOURCE_VIEW_MISSING,
        SOURCE_UNSUPPORTED,
        SOURCE_FILTER_MISSING,
        TARGET_VIEW_MISSING,
        UNSUPPORTED_VIEW_TYPE,
        CONTROLLING_TEMPLATE_MISSING,
        FILTER_DEFINITION_MISSING,
        FILTER_NOT_APPLICABLE,
        FILTER_ALREADY_APPLIED,
        STATE_DIFF,
        OVERRIDE_DIFF,
        VISIBILITY_DIFF,
        ENABLED_DIFF,
        ORDER_DIFF,
        ADD_FILTER_REQUIRED,
        TARGET_FILTERS_CONTROLLED_BY_TEMPLATE,
        FILTER_ENABLED_STATE_UNSUPPORTED,
        FILTER_ORDER_READ_UNSUPPORTED,
        FILTER_ORDER_WRITE_UNSUPPORTED,
        EXACT_SYNC_REMOVE_REQUIRED,
        UNSUPPORTED_FILTER_TYPE,
        INCOMPATIBLE_VIEW,
        STALE_SYNC_PLAN,
        STALE_FILTER_PLAN,
        PREFLIGHT_FAILED,
        UPDATED,
        ADDED,
        REMOVED_FROM_VIEW,
        POST_VERIFY_FAILED,
        CLEAR_FILTER_OVERRIDES,
        APPLIED,
        PARTIAL,
        FAILED,
        BLOCKED
    }
}
